⛔ BLOCKED — BLOCK-LEVEL ISSUES FOUND. DO NOT DEPLOY.

# Portal QA Report — 2026-06-03 06:00 SAST

## PHP Safety  [PASS]

No issues found.

- `portal.php`: Pure HTML shell. Config loaded via `require`. CSP nonce generated with `random_bytes()`. No raw `$_GET`/`$_POST` echoed, no `eval()`, no hardcoded credentials.
- `router.php`: Dev-only file; blocked on production via SAPI check (`die()` if not `cli-server`). No user data reflected to output.
- `reports.php`: Auth gate via `bf_session_start()` + `current_user()` redirect. RBAC enforced per tab with `can()` checks (`safety.view`, `callout.view`, `invoice.view`). `$_GET['tab']` sanitised with `preg_replace('/[^a-z_]/', '')`. All output uses `htmlspecialchars()` via `nilstr()` helper. No raw user input echoed.
- `api/callouts.php`, `api/invoices.php`, `api/payments.php`: All endpoints call `require_auth()` at entry; per-action permission checked via `require_perm()`. All DB queries use parameterised statements (`db_exec`, `db_select`, `db_insert`). Search terms escaped with `like_escape()`. User input sanitised via `clean()`. No `eval()`. Zero endpoints missing an auth guard (checked all files in `api/`).

## JavaScript & CSS  [PASS]

No issues found.

- **innerHTML XSS audit**: All 30+ `innerHTML` assignments confirmed to wrap user-derived API data in `esc()` before insertion. Reviewed `renderCallouts()` (line 2493), `renderInvoices()` (line 2862), `loadAttachments()` (line 347), dashboard compliance alerts (line 2257). Numeric values pass through `fmt()` / `fmtD()`; no raw API field is concatenated unescaped.
- **console.log**: Zero occurrences in `portal.js`. One `console.error('API error:', e)` at line 38 is appropriate error-path instrumentation.
- **CSS naming**: No violations of the `kval--`, `callout--`, or `bf-` naming conventions found in the changed `portal.css`. The `reports.php` embedded CSS uses a self-contained `rpt-*` namespace with no cross-contamination.

## SQL  [ISSUES FOUND]

**🚫 BLOCK — `install/blackfire_testdata_part1.sql` lines 6 and 16**

The file contains the plaintext password for all seeded portal accounts in SQL comments:

```
-- Seeded user password: BlackFire@2026!   (line 6)
-- Seeded password: BlackFire@2026!         (line 16)
```

This credential is committed to the repository in clear text. Anyone with access to the repo or its history can read it. The accounts it covers include admin-role users (`j.shange`, `penny.nzimande`, `z.myeza`, `sibu`).

**Remediation required before next deploy:**
1. Remove the plaintext password from both comment lines — replace with a placeholder such as `-- Seeded password: see secure vault`.
2. Rotate the password on all affected portal accounts in the production and staging databases.
3. Confirm the updated hash is committed without the plaintext annotation.

Other SQL files reviewed:
- `cleanup_data_persons.sql`: All `DELETE` and `UPDATE` statements have `WHERE` clauses. `ALTER TABLE` DDL changes are additive (`ADD COLUMN IF NOT EXISTS`, `ADD CONSTRAINT`). No `DROP TABLE` or `TRUNCATE`. Clean.
- `drop_retired_safety_files.sql`: Six `DELETE FROM` statements, all scoped with `WHERE file_ref IN (...)` or `WHERE ref_id IN (...)`. No `DROP TABLE` or `TRUNCATE`. Clean.
- `fix_notify_emails.sql`: Single `UPDATE … WHERE notify_email = …`. Clean.

## Brand Compliance  [PASS]

No issues found.

- No strings containing `demo`, `Demo`, `TEST`, or `sample` appear in UI-facing code or HTML. The word `Demolition` at `portal.js:4324` is a safety-audit checklist criterion label (`CR14(1) — Demolition Supervisor`), not a brand or placeholder string.
- The portal login button, mobile nav, and all footer links correctly read **"Umlilo Portal"** (`portal.php` lines 129, 142, 229, 278, 351). No alternate platform names used in UI-facing contexts.

## Files Changed in Last 24h

- `BlackFire/BlackFire Portal/api/callouts.php`
- `BlackFire/BlackFire Portal/api/invoices.php`
- `BlackFire/BlackFire Portal/api/payments.php`
- `BlackFire/BlackFire Portal/install/blackfire_testdata_part1.sql` ⚠ BLOCK
- `BlackFire/BlackFire Portal/install/cleanup_data_persons.sql`
- `BlackFire/BlackFire Portal/install/drop_retired_safety_files.sql`
- `BlackFire/BlackFire Portal/install/fix_notify_emails.sql`
- `BlackFire/BlackFire Portal/portal.css`
- `BlackFire/BlackFire Portal/portal.js`
- `BlackFire/BlackFire Portal/reports.php`

## Verdict

BLOCKED — fix issues above ✗

One BLOCK-level finding: plaintext password in `blackfire_testdata_part1.sql` comments (lines 6 and 16). Remove the cleartext credential and rotate the affected accounts before the next deployment. All other checks pass.
