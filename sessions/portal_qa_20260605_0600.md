# Portal QA Report — 2026-06-05 06:00 SAST

## PHP Safety  [PASS]

No issues found.

- `echo $_GET` / `echo $_POST` / `eval()`: None detected across portal.php, router.php, and all api/*.php files.
- RBAC guards: All 21 API endpoint files call `require_auth()` or `require_perm()`. Two exceptions are correct by design:
  - `api/approve.php` — public token-authenticated approval page; validates token via 64-char hex regex and DB lookup.
  - `api/index.php` — returns HTTP 403 unconditionally.
- Prepared statements: All database access goes through parameterised helpers (`db_select`, `db_row`, `db_exec`, `db_insert`) in `includes/db.php`; `PDO::ATTR_EMULATE_PREPARES = false` is enforced. No raw `$pdo->query()` with user-supplied string concatenation found.
- Hardcoded credentials: None. Secrets are read from env vars (`BF_APP_KEY`, `BF_DB_PASS_ENC`, `BF_MAIL_PASS`) or an out-of-webroot `blackfire_secrets.php`; fallback plaintext path is dev-only and emits an `error_log` warning.

## JavaScript & CSS  [PASS]

No issues found.

- `innerHTML` with unsanitised user data: All server-sourced string values rendered via template literals use `esc()` (defined at portal.js:1019 — escapes `&`, `<`, `>`, `"`). Spot-checked callouts table (line 2493), users RBAC table (line 3407), clients table (line 4077), attachments modal (line 347), and compliance dashboard (line 2241). No unescaped user data path found.
- `console.log()`: Zero occurrences in portal.js.
- CSS naming conventions: No PHP/JS/CSS files modified in the last 24 hours — no new CSS classes introduced.

## SQL  [PASS]

No SQL files modified in the last 24 hours.

## Brand Compliance  [PASS]

No issues found.

- `demo` / `Demo` / `TEST` / `sample`: One occurrence of `sample` in `api/user_signature.php:185` — a code comment describing an image-processing algorithm (`// No alpha — sample corners to identify background colour`). Not UI-facing; not a flag.
- Platform naming: The software platform is consistently identified as "Umlilo Portal" across all PHP files, config (`app_name = 'Umlilo Portal'`), and public-facing UI buttons/footer links. No alternative names found on first use.

## Files Changed in Last 24h

- `sessions/portal_qa_20260604_0600.md` (yesterday's QA report — non-code, no review required)

## Verdict

Ready to commit ✓
