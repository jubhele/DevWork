# Portal QA Report — 2026-05-31 06:00 SAST

## PHP Safety  [ISSUES FOUND]

**No BLOCK-level issues.**

1. FLAG — `encrypt_secrets.php` (web root) — Credential-generation utility has no authentication guard. The file itself warns it must be deleted immediately after use ("SECURITY: Never leave this file on a public server" — line 17; UI warning at line 129). If it is still deployed on the live server, any visitor can open it. Recommend confirming deletion from production.

2. FLAG — `api/approve.php:7` — Config include path is incorrect: `require __DIR__ . '/config/config.php'` resolves to the non-existent path `api/config/config.php`. The config lives one level up at `BlackFire Portal/config/config.php`. This file would throw a fatal error if accessed directly. (The equivalent root-level `approve.php` has the same structure and appears to be the intended entry point; the api/ copy may be an accidental duplicate.)

All other PHP safety checks passed:
- No `echo $_GET` / `echo $_POST` / `echo $_REQUEST` found.
- No `eval()` found.
- All `$_GET`/`$_POST` inputs pass through `clean()`, `(int)` casting, or strict regex validation before use.
- No hardcoded credentials — config reads exclusively from env vars and the encrypted `blackfire_secrets.php` file outside web root.
- All PDO connections use `ATTR_EMULATE_PREPARES => false` (true prepared statements throughout).
- RBAC guards present on all authenticated API endpoints. The two un-guarded endpoints are intentional: `api/index.php` returns 403 immediately; `api/approve.php` is a public token-based approval page with strict hex-token validation (`preg_match('/^[a-f0-9]{64}$/i', $token)`).

## JavaScript & CSS  [PASS]

- `esc()` (portal.js:975) correctly HTML-encodes `&`, `<`, `>`, `"` before every `innerHTML` assignment with server-supplied data. No unsanitised interpolation found.
- `escH()` (portal.js:5315) used as a secondary escape helper — also safe.
- No `console.log()` calls found in production code paths.
- No CSS files modified in the last 24 hours — naming-convention check not applicable.

## SQL  [PASS]

No `.sql` files modified in the last 24 hours. No new migrations to review.
All application queries use PDO prepared statements (`ATTR_EMULATE_PREPARES => false`).

## Brand Compliance  [PASS]

- No strings matching `demo`, `Demo`, `TEST`, or `sample` found in UI-facing code or comments in `portal.php` or `portal.js`.
- Platform is correctly named "Umlilo Portal" throughout: nav button (portal.php:129, 142, 229, 279, 351), footer links, and `config.php` `app_name` key. No non-conforming platform names detected.

## Files Changed in Last 24h

- `.gitignore`
- `BlackFire/apps/web/.env.local.example` (submodule pointer — not a portal source file)
- `sessions/2026-05-30_session.md`
- `sessions/portal_qa_20260530_0600.md`
- `umlilo-portal` (git submodule reference)

No portal PHP, JS, CSS, or SQL source files were modified in the last 24 hours.

## Verdict

Ready to commit ✓

Two FLAGs require follow-up before the next production deploy:
1. Confirm `encrypt_secrets.php` is absent from the live server.
2. Investigate whether `api/approve.php` is an accidental duplicate of root `approve.php`; if so, remove it.
