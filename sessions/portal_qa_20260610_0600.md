⛔ BLOCKED — BLOCK-LEVEL ISSUES FOUND. DO NOT DEPLOY.

# Portal QA Report — 2026-06-10 06:00 SAST

## PHP Safety  [ISSUES FOUND]

1. **FLAG — Hardcoded DB username fallback** — `config/config.php:93`
   `'db_user' => getenv('BF_DB_USER') ?: 'blackfm6w9f9_umlilo_admin'`
   The DB username is hardcoded as a fallback. Comment says "fallback for local dev only", but the value is exposed in git history. Move to `.env.example` template and remove from source. Not a BLOCK (no password exposed) but worth resolving before next deploy review.

All other PHP safety checks passed:
- No raw `echo $_GET` / `echo $_POST` / `eval()` anywhere in changed files.
- All API endpoints (`enquiries.php`, `quotes.php`, `statements.php`) open with `require_auth()` and `require_perm()` RBAC guards.
- No hardcoded passwords, API keys, or SMTP secrets — all read via `getenv()` / `cfg_env()` / `bf_decrypt()`.
- `blackfire_secrets.php` and `encrypt_secrets.php` appear in git log but are absent from the working tree (correctly excluded).

## JavaScript & CSS  [PASS]

No issues found.
- Zero `console.log()` calls in portal.js.
- All `innerHTML` assignments (`users-table-body`, `_safUploadCellInner`) pass data through `esc()` which HTML-encodes `& < > "`. No raw user input reaches `innerHTML`.
- `radio.value` passed to `_safUploadCellInner` is constrained to a closed enum (`'To Standard'`, `'Not to Standard'`, `'N/A'`) — not XSS-exploitable.
- CSS class names in portal.css follow established conventions: `bf-`, `kval--`, `saf-`, `pub-`, `callout--`, `dwt-`, `ipanel-`. No convention breaks detected.

## SQL  [ISSUES FOUND]

1. **BLOCK — DELETE without WHERE clause** — `install/rbac_full_migration.sql:20`
   ```sql
   DELETE FROM bf_role_permissions;
   ```
   A full-table delete with no WHERE guard. Per QA policy this is a **BLOCK**. The intent (re-seed the permissions table cleanly) is valid, but the statement must be wrapped in a named transaction with an explicit rollback guard, or replaced with `TRUNCATE bf_role_permissions` inside a transaction.

   **Suggested fix:**
   ```sql
   START TRANSACTION;
   DELETE FROM bf_role_permissions;
   -- INSERT ... re-seed rows follow ...
   COMMIT;
   ```

All other SQL files pass:
- `migration_portal_enquiries.sql` — clean `CREATE TABLE IF NOT EXISTS`, no destructive statements.
- `migration_quote_converted_status.sql` — safe `ALTER TABLE MODIFY COLUMN`.
- `fix_policy_acks_recipient_id.sql` — safe `ALTER TABLE ADD COLUMN IF NOT EXISTS`.
- `fix_statements_defaults.sql` — safe `ALTER TABLE ALTER COLUMN SET DEFAULT`.
- `rbac_full_migration.sql` — remaining INSERTs use `REPLACE INTO` (idempotent). Only the bare DELETE is flagged.
- `blackfire_testdata_part1.sql` — test data only; password hashes are bcrypt (not plaintext); INSERT IGNORE pattern is safe.

## Brand Compliance  [PASS]

No issues found.
- Zero occurrences of `demo`, `Demo`, `TEST`, or `sample` in any UI-facing PHP or JavaScript.
- The software platform is consistently referred to as **"Umlilo Portal"** in portal.php (nav button, footer link, login card) and portal.js. No alternative names detected on first use.

## Files Changed in Last 24h

- `BlackFire/BlackFire Portal/portal.php`
- `BlackFire/BlackFire Portal/portal.js`
- `BlackFire/BlackFire Portal/portal.css`
- `BlackFire/BlackFire Portal/api/enquiries.php`
- `BlackFire/BlackFire Portal/api/quotes.php`
- `BlackFire/BlackFire Portal/api/statements.php`
- `BlackFire/BlackFire Portal/install/rbac_full_migration.sql` ← **BLOCKED here**
- `BlackFire/BlackFire Portal/install/blackfire_testdata_part1.sql`
- `BlackFire/BlackFire Portal/install/migration_portal_enquiries.sql`
- `BlackFire/BlackFire Portal/install/migration_quote_converted_status.sql`
- `BlackFire/BlackFire Portal/install/fix_policy_acks_recipient_id.sql`
- `BlackFire/BlackFire Portal/install/fix_statements_defaults.sql`
- `blackfire_secrets.php`, `encrypt_secrets.php` — in git log but absent from working tree (correctly excluded from deployment)

**Backup compliance:** `BlackFire/BlackFire Portal/_backups/` does not exist. No timestamped backups were created for any of the above files before modification. Per workspace constitution §7a this is mandatory — create `_backups/` and snapshot modified files before the next change cycle.

## Verdict

BLOCKED — fix issues above ✗

**Required before deploy:**
1. Wrap the `DELETE FROM bf_role_permissions` in a transaction (SQL — BLOCK).

**Recommended before next deploy review:**
2. Remove hardcoded `db_user` fallback from `config/config.php` — move to `.env.example`.
3. Create `BlackFire/BlackFire Portal/_backups/` and enforce pre-change backups per §7a.
