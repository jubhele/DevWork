⛔ BLOCKED — BLOCK-LEVEL ISSUES FOUND. DO NOT DEPLOY.

# Portal QA Report — 2026-06-09 06:00 SAST

## PHP Safety  [ISSUES FOUND]

### BLOCK — Hardcoded credentials committed to git
`BlackFire/BlackFire Portal/blackfire_secrets.php` was **added** to the repository in commit `68c7982`
(within the last 24 hours) and contains live credentials in plain PHP:

| Line | Key | Issue |
|------|-----|-------|
| 7 | `BF_DB_PASS_ENC` | Encrypted DB password hardcoded in tracked file |
| 8 | `BF_APP_KEY` | App/encryption key hardcoded — also the key that decrypts line 7 |
| 9 | `BF_MAIL_PASS` | **Plaintext email password** `[REDACTED]` committed to git |
| 10 | `BF_GITHUB_PAT` | **Live GitHub PAT** `github_pat_[REDACTED]` committed to git — **revoke immediately** |

This file must be removed from git tracking (`git rm --cached`) and added to `.gitignore`.
The GitHub PAT is now in git history and should be considered compromised — revoke it at
github.com → Settings → Developer Settings → Personal Access Tokens.

_Note: Yesterday's QA (2026-06-08) flagged the same pattern in `encrypt_secrets.php`.
`blackfire_secrets.php` introduces the same class of violation in a new file._

### PASS — All other PHP safety checks
- `portal.php`: HTML shell only. All dynamic output uses `htmlspecialchars()` or `json_encode()`.
  No `echo $_GET`, `echo $_POST`, or `eval()` detected.
- `router.php`: Dev-only router correctly gated behind `if (PHP_SAPI !== 'cli-server') die(...)`.
  No credential exposure. No unsafe user-input handling.

---

## JavaScript & CSS  [PASS]

1. **console.log** — No `console.log` statements found anywhere in `portal.js`. ✓
2. **innerHTML safety** — All `innerHTML` assignments use the `esc()` helper for user-sourced data.
   Checked: dropdown population (lines 583, 596, 604, 614, 624), ticker (line 2200),
   dashboard render (lines 2360, 2564, 2586), callouts table (line 2822+). No unsafe interpolation found. ✓
3. **CSS naming conventions** — No new CSS class selectors were introduced in the 24h diff to
   `portal.css`. Existing `saf-*` module namespace is consistent and established across the safety module. ✓

---

## SQL  [PASS]

### `install/migration_dashboard_layout.sql`
- No UPDATE or DELETE statements present.
- `CREATE TABLE IF NOT EXISTS bf_settings` — re-run safe; appropriate NOT NULL and DEFAULT constraints. ✓
- `ALTER TABLE bf_users ADD COLUMN dashboard_layout JSON NULL` — guarded by `information_schema` check. ✓
- No DROP TABLE or TRUNCATE. ✓

### `install/migration_doc_numbers.sql`
- All three UPDATE backfills include WHERE clauses (`WHERE quote_no IS NULL OR quote_no = ''` etc.). ✓
- Data-fix UPDATEs (lines 61–78) all scoped by specific ref_id values. ✓
- `INSERT IGNORE` used throughout — safe for re-runs. ✓
- No DROP TABLE or TRUNCATE. ✓

---

## Brand Compliance  [PASS]

1. No `demo`, `Demo`, `TEST`, or `sample` strings found in UI-facing code in `portal.php` or `portal.js`. ✓
2. The portal is referred to as **"Umlilo Portal"** throughout `portal.php` (nav button line 132,
   mobile nav line 145, footer links lines 232, 354). ✓

---

## Files Changed in Last 24h

**Source files modified:**
- `BlackFire/BlackFire Portal/portal.php`
- `BlackFire/BlackFire Portal/portal.css`
- `BlackFire/BlackFire Portal/portal.js`

**Source files added:**
- `BlackFire/BlackFire Portal/blackfire_secrets.php` ← **contains live credentials — see BLOCK above**
- `BlackFire/BlackFire Portal/blackfire_secrets.php.example`

**SQL migrations added:**
- `BlackFire/BlackFire Portal/install/migration_dashboard_layout.sql`
- `BlackFire/BlackFire Portal/install/migration_doc_numbers.sql`

---

## Session Log  [PASS]

`sessions/portal_local_dev_credentials_20260609_025507.md` — Goal, Decisions, Work Done, and
Learnings all present. ✓

---

## Backups  [MISSING]

`BlackFire/BlackFire Portal/_backups/` directory **does not exist** on disk.

The session log references a backup (`_backups/.env_backup_20260609_025507`) but the directory
was not found. The following modified files have no confirmed timestamped backups:

- `BlackFire/BlackFire Portal/portal.php` — no backup found
- `BlackFire/BlackFire Portal/portal.css` — no backup found
- `BlackFire/BlackFire Portal/portal.js` — no backup found

---

## Verdict

**BLOCKED — fix issues above ✗**

### Required before next deploy:
1. **IMMEDIATE** — Revoke the GitHub PAT on line 10 of `blackfire_secrets.php` at github.com.
2. Remove `blackfire_secrets.php` from git tracking: `git rm --cached "BlackFire/BlackFire Portal/blackfire_secrets.php"`
3. Add `blackfire_secrets.php` to `.gitignore` (alongside the existing `blackfire_secrets.php.example` pattern).
4. Verify all secrets in `blackfire_secrets.php` are sourced from `.env` instead.
5. Create `_backups/` and add timestamped backups of `portal.php`, `portal.css`, `portal.js` before
   the next modification cycle (CLAUDE.md §7a).
