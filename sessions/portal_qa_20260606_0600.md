# Portal QA Report — 2026-06-06 06:00 SAST

## PHP Safety  [PASS]
No issues found.

- No raw `echo $_GET` / `echo $_POST` / `eval()` detected across portal root and all api/*.php files.
- All database queries use PDO parameterised statements via `db_select()`, `db_row()`, `db_insert()`, `db_exec()` helpers. No string-interpolated SQL found.
- Every API endpoint requires authentication via `require_auth()` before any data access. Endpoints that mutate state call `require_perm()` with the correct permission key.
- Config (`config/config.php`) reads credentials exclusively from environment variables or the external `blackfire_secrets.php` file (one level above `public_html`). No hardcoded passwords, API keys, or encryption secrets in committed code.
- `includes/auth.php` implements CSRF token generation and `verify_csrf()` for state-changing requests; session flags (httponly, samesite, secure-when-https) are correctly set.

## JavaScript & CSS  [PASS]
No issues found.

- `esc()` is defined at portal.js:1019 as a full HTML entity encoder (`&`, `<`, `>`, `"`) and is applied consistently to all user-derived data in `innerHTML` template literals throughout the file.
- Spot-checked innerHTML at lines 347, 529, 550, 1864, 2493, 2713, 2862, 2968, 3332, 3376, 3407, 4077, 4190 — all interpolated values wrapped with `esc()` or computed from numeric/boolean types.
- Line 6490: `title` contains only hardcoded string literals and integer counts — no user input path.
- No `console.log()` calls found. `console.error()` at api() (line 38) is appropriate for the error-handling code path.
- CSS class names follow the established `bf-`, `kval--`, `kcard--` conventions. No violations found in portal.css (766 rules reviewed).

## SQL  [PASS]
No issues found.

No `.sql` files were modified in the last 24 hours. No new SQL to review.

## Brand Compliance  [PASS]
No issues found.

- The portal is consistently referred to as **"Umlilo Portal"** in all UI-facing strings: nav button (portal.php:129), mobile nav (portal.php:142), footer links (portal.php:229, 278, 351), and the `app_name` config key (config/config.php:93).
- The string "Demolition Supervisor" at portal.js:4324 is a safety checklist criteria label — not a demo/test marker.
- The word "sample" at api/user_signature.php:185 appears in an internal code comment describing an image-processing algorithm — not UI-facing.
- No occurrences of bare `demo`, `Demo`, `TEST`, or `sample` in UI-facing code or user-visible strings.

## Files Changed in Last 24h
- None

## Verdict
Ready to commit ✓
