⛔ BLOCKED — BLOCK-LEVEL ISSUES FOUND. DO NOT DEPLOY.

# Portal QA Report — 2026-06-08 06:00 SAST

## PHP Safety  [ISSUES FOUND]

**BLOCK — Hardcoded database credentials in tracked file**
- `BlackFire/BlackFire Portal/encrypt_secrets.php` is tracked by git despite a `.gitignore` entry for it.
- Lines 65–66 of that file contain hardcoded production values inside a heredoc template:
  - `BF_DB_NAME = 'blackfm6w9f9_portal'`
  - `BF_DB_USER = 'blackfm6w9f9_umlilo_admin'`
- A DB username committed to source control is a credential exposure. The file must be untracked (`git rm --cached`) and `.gitignore` must be verified effective.
- The file is a one-time secrets generator — the comment inside says "DELETE THIS FILE immediately after use." It has not been deleted.

**PASS — All other PHP safety checks**
- No `echo $_GET` / `echo $_POST` / `eval()` found in any production PHP file.
- All 24 API endpoints call `require_auth()` or `require_perm()` at the file or action level.
- `clean()` helper in `includes/helpers.php` consistently applied to GET/POST string inputs before storage.
- All SQL queries use parameterised statements (PDO prepared + bound params); no interpolation of user data into query strings detected.

## JavaScript & CSS  [PASS]

- `esc()` sanitizer (`String(s).replace(…)`) is defined at line 1019 of `portal.js` and is applied to all server-sourced field values before insertion via `innerHTML`.
- The one `innerHTML` assignment that does not call `esc()` (line 2235 — dash alert strip) uses only server-returned integer counts, not user-controlled strings. Safe.
- No `console.log()` calls found in `portal.js`; only `console.error()` inside the API catch block (acceptable for error surfacing).
- No CSS class names found that violate the `kval--`, `callout--`, or `bf-` naming conventions.

## SQL  [PASS]

- No files modified in last 24 hours contain SQL statements.
- `DROP TABLE` and `TRUNCATE` appear only in `install/` migration scripts — not in production API code.
- All `UPDATE` and `DELETE` statements in production API files include parameterised `WHERE` clauses.

## Brand Compliance  [ISSUES FOUND]

**BLOCK — Test scaffold committed to repo root, visible at web root**
- `portal.php` (repo root, git-tracked) is a test scaffolding file that must not be deployed.
  - Line 6: `<title>Umlilo Portal - Test View</title>` — contains "Test View"
  - Line 84: `<div class="logo">Umlilo Portal Test</div>` — contains "Test"
  - Lines 99–106: Eight sidebar links labelled `TEST LINK 1` through `TEST LINK 8`
  - Line 110: `<h1>Umlilo Portal Testing Ground</h1>` — contains "Testing Ground"
- This file is tracked by git (`git ls-files portal.php` confirms) and would be deployed as-is. It must be removed from the repo or moved to `dev-only/`.

**FLAG — Product name usage in root portal.php**
- The file refers to the platform as "Umlilo Portal Test" and "Umlilo Portal Testing Ground" — neither is the correct first-use form "the Umlilo Portal". (Moot given the BLOCK above, but noted.)

**PASS — Main portal files**
- `BlackFire/BlackFire Portal/portal.php`, `portal.js`, and `portal.css` contain no 'demo', 'Demo', 'TEST', or 'sample' strings in UI-facing code (one hit — 'Demolition Supervisor' — is a safety-checklist criterion, not UI labelling).
- Platform is referred to consistently as "Umlilo Portal" throughout the authenticated shell.

## Files Changed in Last 24h
- `sessions/portal_qa_20260607_0600.md` (previous day QA report — no production code changes)

## Verdict
BLOCKED — fix issues above ✗

### Required before next deploy
1. **BLOCK — PHP:** Remove `encrypt_secrets.php` from git tracking:
   ```
   git rm --cached "BlackFire/BlackFire Portal/encrypt_secrets.php"
   git commit -m "chore: untrack encrypt_secrets.php (gitignored setup tool)"
   ```
2. **BLOCK — Brand/Test:** Remove or relocate `portal.php` from the repo root:
   ```
   git rm portal.php
   git commit -m "chore: remove test scaffold from repo root"
   ```
   If the file is still needed for local dev reference, move it to `BlackFire/BlackFire Portal/dev-only/`.
