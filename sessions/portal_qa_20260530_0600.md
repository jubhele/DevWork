# Portal QA Report — 2026-05-30 06:00 SAST

## PHP Safety  [PASS]

No issues found.

- All API endpoints (`safety.php`, `safety_doc_gen.php`, `safety_personnel.php`, `safety_policy.php`) guarded by `require_auth()` followed by `require_perm()` on every route.
- `reports.php` gates page access by `can('safety.view')`, `can('callout.view')`, and `can('invoice.view')`/`can('finance.income')` — users with no matching permission are redirected with 403.
- No raw `echo $_GET`, `echo $_POST`, or `eval()` found in any changed file.
- All user-supplied input passes through `clean()`, `filter_var()`, or cast to `int`/`float` before use in SQL prepared statements.
- No hardcoded credentials, passwords, or API keys detected. SMTP and portal config are referenced via `$cfg` from `config/config.php` (excluded from git per `.gitignore`).
- **FLAG (low)** — `band_pill()` and `status_pill()` in `reports.php` (lines 246–261) embed database values in HTML without `htmlspecialchars()`. Risk is low — values are sanitised by `clean()` at write time and `band` values are produced by a SQL `CASE` statement — but defence-in-depth escaping is missing. Not a BLOCK; worth fixing at next touch.

## JavaScript & CSS  [PASS]

No BLOCK-level issues found.

- **innerHTML safety:** All dynamic content rendered via `innerHTML` in `portal.js` consistently uses the `esc()` helper for user-originated data (client names, callout descriptions, usernames, appointee names, comments, etc.). Static HTML templates hardcoded in template literals contain no user data. No unsanitised injection path found.
- **console.log:** Zero occurrences across the full `portal.js` file.
- **CSS naming conventions:** The only new class added to `portal.css` in the last 24 h is `.hidden` (a reset utility). The three classes added in the `bd72205` commit — `.saf-rpt-ev-hdr`, `.saf-rpt-ev-cell`, `.saf-rpt-no-ev` — all conform to the `saf-` prefix convention.

## SQL  [PASS]

- `install/safety_officer_migration.sql` — contains only `INSERT IGNORE INTO bf_role_permissions`, `ALTER TABLE bf_users MODIFY COLUMN role ENUM(...)`, and a commented-out example `INSERT INTO bf_users`. No `UPDATE`, `DELETE`, `DROP TABLE`, or `TRUNCATE` statements present.

## Brand Compliance  [PASS]

- Platform is referred to as **"Umlilo Portal"** on every first-use occurrence in `portal.php` (nav button, mobile nav, footers). Consistent with brand requirement.
- Searches for `demo`, `Demo`, `TEST`, `sample` across changed files returned only false positives: "Demolition Supervisor" (legal appointment label), "drug testing" and "roadworthy testing station" (safety document copy). No UI-facing branding violations.

## Session Log  [NEEDS UPDATE]

- `sessions/2026-05-30_session.md` exists but the **Goal, Decisions, and Work Done sections are empty**. The session appears to have ended without `/learn` being run.
- Action required: fill in Goal and Work Done before the next coding session begins, then run `/learn` to persist learnings.

## Backups  [MISSING — CONSTITUTION VIOLATION]

- No `_backups/` directories found anywhere in the repository.
- **12 files modified in the last 24 h have no timestamped backups**, in violation of CLAUDE.md § 7a:
  - `BlackFire/BlackFire Portal/portal.css`
  - `BlackFire/BlackFire Portal/portal.js`
  - `BlackFire/BlackFire Portal/router.php`
  - `BlackFire/BlackFire Portal/reports.php`
  - `BlackFire/BlackFire Portal/api/safety.php`
  - `BlackFire/BlackFire Portal/api/safety_doc_gen.php`
  - `BlackFire/BlackFire Portal/api/safety_personnel.php`
  - `BlackFire/BlackFire Portal/api/safety_policy.php`
  - `BlackFire/BlackFire Portal/install/safety_officer_migration.sql`
  - `BlackFire/BlackFire Portal/dev-only/bhekani_bo.php` (dev-only)
  - `BlackFire/BlackFire Portal/dev-only/generate_attachment_placeholders.php` (dev-only)
  - `BlackFire/BlackFire Portal/dev-only/import_safety_docs.php` (dev-only)
- Recommendation: create `BlackFire/BlackFire Portal/_backups/` and `BlackFire/BlackFire Portal/api/_backups/` and back-fill copies before the next modification cycle.

## Files Changed in Last 24h

- `BlackFire/BlackFire Portal/api/safety.php`
- `BlackFire/BlackFire Portal/api/safety_doc_gen.php`
- `BlackFire/BlackFire Portal/api/safety_personnel.php`
- `BlackFire/BlackFire Portal/api/safety_policy.php`
- `BlackFire/BlackFire Portal/dev-only/bhekani_bo.php`
- `BlackFire/BlackFire Portal/dev-only/generate_attachment_placeholders.php`
- `BlackFire/BlackFire Portal/dev-only/import_safety_docs.php`
- `BlackFire/BlackFire Portal/dev-only/portal - Copy.php`
- `BlackFire/BlackFire Portal/dev-only/refactored_portal/includes/portal_footer.php`
- `BlackFire/BlackFire Portal/dev-only/refactored_portal/includes/portal_header.php`
- `BlackFire/BlackFire Portal/install/safety_officer_migration.sql`
- `BlackFire/BlackFire Portal/portal.css`
- `BlackFire/BlackFire Portal/portal.js`
- `BlackFire/BlackFire Portal/reports.php`
- `BlackFire/BlackFire Portal/router.php`

## Verdict

Ready to commit ✓

No BLOCK-level security issues found. Two items require follow-up before next deploy cycle:
1. **FLAG** — Add `htmlspecialchars()` escaping to `band_pill()` / `status_pill()` in `reports.php`.
2. **Constitution** — Create `_backups/` directories and maintain timestamped backups per CLAUDE.md § 7a.

## Learnings
âš  /learn was not run before this session ended.
Action required at next session start: review this log and run /learn (Claude Code)
or manually update memory/ files (all other providers) before new work begins.

_Session ended: 2026-05-30 20:20:29 (Claude Code / claude-sonnet-4-6)_
