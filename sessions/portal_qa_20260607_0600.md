# Portal QA Report — 2026-06-07 06:00 SAST

## PHP Safety  [PASS]
No issues found.

- No raw `echo $_GET` / `echo $_POST` anywhere in portal.php, router.php, or api/*.php
- No `eval()` usage detected
- All API endpoints use `require_auth()` or `require_perm()` before processing — RBAC guards confirmed present across admin.php, users.php, invoices.php, quotes.php, callouts.php, safety.php and all other endpoints
- User input consistently sanitised via `clean()` (helpers.php:46), `(int)` casting, or `require_perm()` before use in SQL
- All SQL operations use prepared statements via `db_exec()` / `db_select()` — no string-interpolated queries executed against the DB
- No hardcoded credentials, passwords, or API keys found in any PHP file
- Note (informational): admin.php:44 generates SQL strings via interpolation for export/display only — actual DB writes on lines below use prepared statements; the comment correctly explains bcrypt output is ASCII-safe. Not a security finding.

## JavaScript & CSS  [PASS]
No issues found.

- `esc()` function (portal.js:1019) and `escH()` (portal.js:5745) are consistently applied to all user-controlled or API-sourced strings before innerHTML assignment — verified across tables, modals, ticker, nav, attachment panels, compliance widgets, and all dynamic renders
- Ticker HTML (line 1856) built from static `SERVICES` array with `esc()` — safe
- Nav labels (line 943) built from static `NAV_CONFIG` — not user-controlled
- `strip.innerHTML = extra + strip.innerHTML` (line 2235) inserts numeric counts only — safe
- No `console.log()` calls found anywhere in portal.js
- CSS class naming conventions intact: `kval--`, `kcard--`, `bf-`, `saf-`, `pnav`, `pill`, `btn` prefixes all follow established project pattern — no new classes found that break naming conventions

## SQL  [PASS]
No issues found.

- No `UPDATE` or `DELETE` without a `WHERE` clause detected in any api/*.php file
- No `DROP TABLE` or `TRUNCATE` statements found anywhere in the codebase

## Brand Compliance  [PASS]
No issues found.

- "Demolition Supervisor" (portal.js:4324, api/safety_doc_gen.php:136) is a legitimate SHE/construction role in safety checklist data — not a UI branding string
- "sampled" in api/user_signature.php:136 appears in a code comment describing image processing — not a UI-facing string
- Portal consistently referred to as "Umlilo Portal" on all first-use occurrences in portal.php (lines 129, 142, 229, 351) and throughout portal.js
- No "TEST" or "sample" strings in UI-facing code

## Files Changed in Last 24h
- sessions/portal_qa_20260606_0600.md (yesterday's QA report — no code changes)

## Verdict
Ready to commit ✓
