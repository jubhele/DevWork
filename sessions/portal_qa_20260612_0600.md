# Portal QA Report — 2026-06-12 06:00 SAST

## PHP Safety  [PASS]
No issues found.

- No `echo $_GET` / `echo $_POST` / `eval()` anywhere in portal or API files.
- All `$_GET` / `$_POST` inputs are sanitised via `clean()`, `(int)` casting, `preg_replace()` with strict allow-lists, or `filter_var()` before use. No raw passthrough to output or queries.
- All API endpoints (`api/*.php`) are guarded by `require_auth()` or `require_perm()` at the point of access. `require_perm()` internally calls `require_auth()` and dies 401/403 on failure.
- Token-based flows (`api/approvals.php` GET/PATCH, `external_upload.php`, `sign.php`, `policy_ack.php`) use URL-token validation as their auth mechanism — these are intentionally public endpoints and the docblocks confirm this.
- `api/admin.php` lines 38 and 113 interpolate a bcrypt hash and a username from the database into a SQL string for **export preview only** — not executed. Actual DB writes use `db_exec()` with prepared statements. No injection risk.
- No hardcoded credentials, passwords, or API keys found in any `.php` file.

## JavaScript & CSS  [PASS]
No issues found.

- `esc()` helper is defined at `portal.js:1152` and `portal.js:6014`; `escH()` at `portal.js:6340`. All user-derived string interpolations inside `innerHTML` template literals consistently call `esc()`.
- Static/internal-data `innerHTML` assignments (nav items, hardcoded HTML fragments, ticker duplication at line 2239) do not involve user input — no injection path.
- No `console.log()` calls found in any `.js` file.
- No new CSS class names violating `kval--`, `callout--`, or `bf-` naming conventions. The Safety module introduces `saf-*` classes consistent with the established extension pattern.

## SQL  [PASS]
No issues found.

- No `.sql` files modified in the last 24 hours.
- All PHP database calls use `db_exec()` / `db_select()` with parameterised queries. No `UPDATE` or `DELETE` without `WHERE` clauses found in the PHP query layer.

## Brand Compliance  [PASS]
No issues found.

- No UI-facing strings containing `demo`, `Demo`, `TEST`, or `sample`. One occurrence in a PHP source comment (`api/user_signature.php:185: // No alpha — sample corners to identify background colour`) — not user-visible.
- The portal login button (`portal.php:132`) and all footer links use "Umlilo Portal" as the product name on first and subsequent use. No alternate naming found in UI-facing code.

## Files Changed in Last 24h
- None

## Verdict
Ready to commit ✓
