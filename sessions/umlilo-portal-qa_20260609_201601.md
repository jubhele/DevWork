# Session: Umlilo Portal QA
Date: 2026-06-09
Provider: OpenAI Codex
Model: GPT-5 Codex

## Goal
Run a credentialed QA pass on `c:\DevWork\umlilo-portal` using test users from `c:\DevWork\umlilo-portal\.env`, preserving secret values and avoiding source changes unless explicitly approved after findings.

## Model Recommendation
Task tier: 3-Complex
Recommended model: codex + reasoning  Trust score: 8/10
Active model: GPT-5 Codex  Status: correct

## Decisions
- Used the `qa` skill workflow as a non-mutating QA pass because `c:\DevWork\umlilo-portal` had pre-existing uncommitted/untracked work before testing.
- Used `.env` only inside automation for credentialed login attempts; no passwords or secret values were printed or written into reports.
- Fell back from the in-app Browser plugin to scratch Playwright automation because the plugin-reported `iab` browser was not available in this session.

## Work Done
- Started the local Next.js web app with `pnpm dev:web`, tested `http://localhost:3000`, and stopped the dev-server process tree before ending.
- Created credentialed browser QA artifacts at `c:\DevWork\umlilo-portal\.gstack\qa-reports\qa-report-localhost-3000-2026-06-09.md`, `baseline-localhost-3000-2026-06-09.json`, and screenshots under `.gstack\qa-reports\screenshots\20260609_203217\`.
- Tested `.env` account roles: admin, safety, compliance, and inspector.
- Covered `/dashboard`, `/callouts`, `/quotes`, `/invoices`, `/audit`, `/privacy`, and mobile viewport checks for dashboard/callouts.
- Ran `pnpm --filter web test:agent` (pass), `pnpm --filter web lint` (fail: 61 problems), and `pnpm --filter web build` (pass).
- Updated workspace memory at `C:\Users\Jughele Shange\.claude\projects\c--DevWork\memory\project_umlilo_portal.md` with durable QA findings.

## Blockers / Next Steps
- QA found 6 issues, no fixes applied in this session: inspector login fails, `/quotes` and `/invoices` return 500, `/audit` is linked but missing, `/dashboard` cannot load data, and legacy `portal.js` produces runtime errors.
- Fix pass should start from the report's ISSUE-001 through ISSUE-006, with backups before any source edits and retesting after each fix.
- Existing repo worktree was dirty before QA; preserve those changes and avoid bundling QA fixes with unrelated constitution/artifact work.

## Learnings
- Runtime QA is required even when `next build` passes: `/quotes` and `/invoices` compile but fail at request time because the data shape lacks `total`.
- The app still mixes legacy `portal.js` assumptions with Next pages; relative asset loading from `/login?next=%2F` and DOM assumptions in `buildHomeCats` create browser console errors.
- Model trust score unchanged: GPT-5 Codex was appropriate for this Tier 3 QA/debugging task.

## Resumed 2026-06-09 — Learning Capture
- Added repo-local QA lessons at `c:\DevWork\umlilo-portal\memory\project_qa_lessons.md`.
- Updated `c:\DevWork\umlilo-portal\memory\MEMORY.md` to index the QA lessons.
- Added a `QA Process Lessons (2026-06-09)` section to workspace memory at `C:\Users\Jughele Shange\.claude\projects\c--DevWork\memory\project_umlilo_portal.md`.
- Created 6 searchable gstack learning entries under `C:\Users\Jughele Shange\.gstack\projects\jubhele-umlilo-portal\learnings.jsonl`.
- The official `gstack-learnings-log` Bash script could not validate entries in this PowerShell/Git-Bash/Bun path, so entries were written as schema-compatible JSONL after validation with Node.

```json
{
  "session_id": "20260609_201601",
  "agent": "Mvavanyi",
  "model_endpoint": "gpt-5-codex",
  "token_metrics": {
    "tokens_in": 0,
    "tokens_out": 0,
    "iteration_count": 0
  },
  "outcome": {
    "status": "SUCCESS",
    "cost_category": "TIER_3_HIGH"
  },
  "optimization": {
    "action_taken": "None"
  }
}
```
