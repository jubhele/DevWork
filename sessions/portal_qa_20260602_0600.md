⛔ BLOCKED — BLOCK-LEVEL ISSUES FOUND. DO NOT DEPLOY.

# Portal QA Report — 2026-06-02 06:00 SAST

## PHP Safety  [PASS]

No issues found.

- No raw `echo $_GET` / `echo $_POST` / `echo $_REQUEST` in any API or include file.
- No `eval()` calls anywhere in the codebase.
- No hardcoded credentials, passwords, or API keys detected.
- All API endpoints are properly guarded:
  - `api/auth.php` — uses `current_user()` / `require_auth()` per action.
  - `api/callouts.php` — `require_auth()` at entry; `require_perm('callout.view/create/confirm_closure')` per action.
  - `api/clients.php` — `require_auth()` at entry; `require_perm('clients.view/create/update')` per action.
  - `api/users.php` — `require_perm('security.users')` / `require_perm('user.create/update')` per action.
  - `api/safety.php` — `require_auth()` + `require_perm('safety.view')` at file entry; `require_perm('safety.create/update/delete')` on mutations.
  - `api/safety_compliance.php` — `require_auth()` + `require_perm('safety.view')` at entry.
  - `api/safety_doc_gen.php` — `require_auth()` + `require_perm('safety.view')` at entry.
  - `api/safety_personnel.php` — `require_auth()` + `require_perm('safety.view')` at entry; `require_perm('safety.delete')` on delete.
  - `api/safety_policy.php` — `require_auth()` + `require_perm('safety.view')` at entry; `require_perm('safety.create')` on mutations.
  - `api/files.php` — `require_auth()` at entry.
  - `api/user_signature.php` — `require_perm('user.update')` at entry.

---

## JavaScript & CSS  [PASS]

No blocking issues found. One FLAG.

- All `innerHTML` assignments that render server or user data use the `esc()` helper (`portal.js:990`), which HTML-encodes `&`, `<`, `>`, and `"`. Pattern is consistent throughout the file.
- No `console.log()` calls detected in `portal.js`.
- No new CSS classes that break the `kval--`, `callout--`, or `bf-` naming conventions. The safety module uses the established `saf-` prefix throughout `portal.css`.

**FLAG** — `portal.js:1216`
> `'Not directly in this portal version.'`
The platform is referred to informally as "this portal version" in a user-facing Page Guide FAQ response. Per brand rules, the first contextual reference should read **"the Umlilo Portal"**.

---

## SQL  [ISSUES FOUND]

**BLOCK** — `install/blackfire_real_finance_data.sql:33`
```sql
DELETE FROM bf_transactions;
```
Unconditional `DELETE` with no `WHERE` clause. Full table wipe. Even in a seeding script this pattern is a BLOCK per QA policy.

**BLOCK** — `install/migration_finalize_person_cleanup.sql:83`
```sql
DELETE FROM bf_policy_acks;
```
Unconditional `DELETE` with no `WHERE` clause. Comment explains intent ("All existing rows are seed/test data — clear before schema change"), but the pattern still triggers a BLOCK.

**WARN** — `install/drop_seed_backups.sql:75`
```sql
SET @sql = IF(@tables IS NOT NULL, CONCAT('DROP TABLE IF EXISTS ', @tables), ...);
PREPARE stmt FROM @sql;
EXECUTE stmt;
```
Dynamic `DROP TABLE IF EXISTS` via a prepared statement. Not wrapped in `START TRANSACTION` / `COMMIT`. If the script fails mid-run there is no rollback. Acceptable for a manual cleanup script, but should carry a `WARN` per policy.

All other `UPDATE` and `DELETE` statements in the changed SQL files have proper `WHERE` clauses. ✓

---

## Brand Compliance  [ISSUES FOUND]

**FLAG** — `portal.js:1216`
The Page Guide FAQ answer for "Can clients view their own statements?" reads:
> *"Not directly in this portal version."*
Should be: *"Not directly in the Umlilo Portal at this time."*
(Applies the brand name on first contextual use.)

No occurrences of `demo`, `Demo`, `TEST`, or `sample` in UI-facing code or HTML strings. Matches in `blackfire_testdata_part1.sql` are SQL comments inside an install-only seeding file — not UI-facing. ✓

---

## Files Changed in Last 24h

```
BlackFire/BlackFire Portal/api/auth.php
BlackFire/BlackFire Portal/api/callouts.php
BlackFire/BlackFire Portal/api/clients.php
BlackFire/BlackFire Portal/api/files.php
BlackFire/BlackFire Portal/api/safety.php
BlackFire/BlackFire Portal/api/safety_compliance.php
BlackFire/BlackFire Portal/api/safety_doc_gen.php
BlackFire/BlackFire Portal/api/safety_personnel.php
BlackFire/BlackFire Portal/api/safety_policy.php
BlackFire/BlackFire Portal/api/user_signature.php
BlackFire/BlackFire Portal/api/users.php
BlackFire/BlackFire Portal/includes/auth.php
BlackFire/BlackFire Portal/install/blackfire_real_finance_data.sql
BlackFire/BlackFire Portal/install/blackfire_testdata_part1.sql
BlackFire/BlackFire Portal/install/cleanup_data_persons.sql
BlackFire/BlackFire Portal/install/deduplicate_clients_by_name.sql
BlackFire/BlackFire Portal/install/drop_seed_backups.sql
BlackFire/BlackFire Portal/install/fix_aeci_client_dedup.sql
BlackFire/BlackFire Portal/install/fix_aeci_personnel_backfill.sql
BlackFire/BlackFire Portal/install/fix_remove_nonfile_personnel.sql
BlackFire/BlackFire Portal/install/fix_saf_120326_0001_personnel.sql
BlackFire/BlackFire Portal/install/fix_schema_and_cleanup.sql
BlackFire/BlackFire Portal/install/migration_all_person_fk.sql
BlackFire/BlackFire Portal/install/migration_client_contacts.sql
BlackFire/BlackFire Portal/install/migration_drop_person_text_columns.sql
BlackFire/BlackFire Portal/install/migration_drop_personnel_text_cols.sql
BlackFire/BlackFire Portal/install/migration_finalize_person_cleanup.sql
BlackFire/BlackFire Portal/install/migration_personnel_user_fk.sql
BlackFire/BlackFire Portal/install/migration_safety_files_user_fk.sql
BlackFire/BlackFire Portal/install/migration_user_roles.sql
BlackFire/BlackFire Portal/install/migration_user_signatures.sql
BlackFire/BlackFire Portal/install/update_saf_120326_0001_may2026.sql
BlackFire/BlackFire Portal/portal.css
BlackFire/BlackFire Portal/portal.js
BlackFire/BlackFire Portal/portal.php
sessions/2026-06-01_session.md
sessions/blackfire_client_contacts_20260531_200212.md
sessions/blackfire_dedup_clients_20260531_194329.md
sessions/multi_role_users_20260601_105628.md
sessions/portal_qa_20260601_0600.md
sessions/portal_signature_upload_fix_20260601_103547.md
sessions/token_digest_weekly.md
sessions/update_saf_120326_0001_may2026_20260531_200212.md
```

---

## Verdict

BLOCKED — fix issues above ✗

**To unblock:**
1. **SQL BLOCK ×2** — Add a `WHERE` clause to the two unconditional `DELETE` statements, or wrap them in a clearly commented `-- INTENTIONAL FULL WIPE` block with an explicit transaction (`START TRANSACTION; DELETE FROM ...; COMMIT;`) so the intent is documented and the operation is atomic. The QA rule will remain BLOCK until the pattern is resolved.
2. **Brand FLAG** — Update `portal.js:1216`: replace `"this portal version"` with `"the Umlilo Portal"`.
3. **SQL WARN** — Optionally wrap the dynamic `DROP TABLE` in `drop_seed_backups.sql` in a `START TRANSACTION` / `COMMIT` block.

## Learnings
âš  /learn was not run before this session ended.
Action required at next session start: review this log and run /learn (Claude Code)
or manually update memory/ files (all other providers) before new work begins.

_Session ended: 2026-06-02 06:32:32 (Claude Code / claude-sonnet-4-6)_
_Session ended: 2026-06-02 07:47:25 (Claude Code / claude-sonnet-4-6)_
_Session ended: 2026-06-02 07:49:26 (Claude Code / claude-sonnet-4-6)_
_Session ended: 2026-06-02 07:58:21 (Claude Code / claude-sonnet-4-6)_
_Session ended: 2026-06-02 08:12:31 (Claude Code / claude-sonnet-4-6)_
