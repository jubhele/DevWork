# Copy this to .env.local — never commit .env.local

# PHP backend base URL (no trailing slash)
NEXT_PUBLIC_API_BASE=https://blackfiresolutions.co.za/api

# For local dev against the local PHP server
# NEXT_PUBLIC_API_BASE=http://127.0.0.1:8080/api
