# BlackFire Solutions Portal — config/ directory protection
# This MUST remain here. config.php contains database credentials.
# Direct web access to this directory is denied at all times.

Order deny,allow
Deny from all

<FilesMatch ".*">
    Order deny,allow
    Deny from all
</FilesMatch>
