<?php
/**
 * Umlilo Portal — Production secrets template
 *
 * Copy to:  /home/blackfm6w9f9/blackfire_secrets.php
 *           (one level ABOVE public_html — never web-accessible)
 *
 * NEVER commit the real file to git. GitHub's secret scanner revokes
 * committed tokens, and production tags must not point at secret-bearing
 * files. This template is the only version that lives in the repo.
 *
 * config/config.php loads this file first; constants defined here are
 * the production source of truth for all credentials.
 */

// ── Database ────────────────────────────────────────────────
define('BF_DB_HOST', 'localhost');
define('BF_DB_PORT', 3306);
define('BF_DB_NAME', '');           // e.g. cpaneluser_portal
define('BF_DB_USER', '');           // e.g. cpaneluser_dbuser  ← REQUIRED since 2026-06-11 (hardcoded fallback removed)
define('BF_DB_PASS_ENC', '');       // base64( IV[16] || AES-256-CBC ciphertext ) from install/encrypt_config.php

// ── GitHub (deploy.sh) ──────────────────────────────────────
// Fine-grained PAT scoped to jubhele/BlackFire, Contents: Read and write
define('BF_GITHUB_PAT', '');

// ── SMTP ────────────────────────────────────────────────────
define('BF_SMTP_HOST', '');
define('BF_SMTP_PORT', 587);
define('BF_SMTP_USER', '');
define('BF_SMTP_PASS_ENC', '');
