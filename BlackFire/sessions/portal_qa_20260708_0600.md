# Portal QA Report — 2026-07-08 06:00 SAST

## PHP Safety  [PASS]

No issues found.

- `portal.php`: Pure HTML shell. All dynamic output (`$cfg` values) is wrapped in `htmlspecialchars()` or `json_encode()`. No `$_GET`/`$_POST` echoed. No `eval()`. No hardcoded credentials.
- `router.php`: Dev-only routing utility. No SQL, no echoed user input, no credentials.
- RBAC is enforced at the API layer (api/*.php endpoints); the portal shell carries no server-side gated sections that could be bypassed.

## JavaScript & CSS  [ISSUES FOUND]

**FLAG — CSS `background-size: auto` on PNG reference**

- `portal.css:63` — `body::before` references `blackfire_logo_transparent.png` with `background-size:280px auto`. The height dimension is `auto`, which violates the project rule ("always set explicit `background-size` on large PNGs — never `auto`"). Recommend `280px 280px` or the exact natural dimensions.
- `portal.css:105` — `.izilo-band` uses `background-size:auto 100%`. Targets inline SVG data URIs, not PNGs, so this falls outside the strict rule scope — noted for awareness only.

No other issues found:

- `innerHTML` assignments: All dynamic data is sanitised through `esc()` before injection. Spot-checked: dropdown option builders (lines 663–732), compliance widget table (line 2876), tracker updates (line 3430), attachment lists (line 456). No unsanitised user-controlled string reaches the DOM.
- `console.log()`: Zero occurrences in `portal.js`.
- CSS naming conventions (`kval--`, `callout--`, `bf-`): No violations in reviewed classes.

## SQL  [PASS]

No `.sql` files modified in the last 24 hours. No SQL statements present in `portal.php` or `router.php`.

## Brand Compliance  [PASS]

No issues found.

- Searches for `demo`, `Demo`, `TEST`, `sample` in `portal.php`, `portal.js`, and `portal.css` returned no UI-facing hits. ("TESTIMONIALS" is a section label, not a brand violation; "Demolition Supervisor" is a legitimate SHE audit criterion.)
- Platform naming: All body copy and FAQ answers refer to the platform as "the Umlilo Portal" on first use (portal.php lines 296, 300, 374, 421, 447). ✓

## Files Changed in Last 24h

- `sessions/portal_qa_20260707_0600.md`

## Verdict

Ready to commit ✓

No BLOCK-level issues found. One FLAG-level CSS issue pre-exists in `portal.css:63` (`background-size:280px auto` on the logo PNG). No PHP/JS/CSS production files were modified in the last 24 hours — this issue carries over from prior sessions. Recommend a targeted fix in the next Umakhi sprint.
