# Portal QA Report — 2026-07-21 06:00 SAST

## PHP Safety  [PASS]
No issues found.

- `echo $_GET` / `echo $_POST` / `eval()`: not present in any reviewed PHP file.
- All SQL calls use parameterized queries via `db_select()`, `db_row()`, `db_exec()`, `db_insert()` — no raw string interpolation into queries.
- RBAC guards confirmed on both changed API endpoints:
  - `api/statements.php`: `api_headers()` → `require_auth()` → `can()` permission checks on every branch (GET list, GET download, GET email_options, POST generate, PUT release). Cron branch is secret-gated before auth.
  - `api/task_users.php`: `api_headers()` → `require_auth()` → `can('task.create') && can('task.update')` gate. `can()` without explicit role arg correctly resolves to the current session user — intended design confirmed in `auth.php`.
- No hardcoded credentials, passwords, or API keys in any PHP file. `deploy.sh` reads the GitHub PAT at runtime from `~/blackfire_secrets.php` or `~/.bf_pat`.
- `portal.php` is a pure HTML shell — no server-side data output from superglobals.

## JavaScript & CSS  [PASS]
No issues found.

- `innerHTML` assignments: `esc()` is defined three times across scopes (line 1643 global, line 9186 and 10835 local scopes) and consistently applied to all server-data values before injection. No unsanitised user data injected via `innerHTML`.
- `console.log()`: zero matches in `portal.js`.
- CSS naming: no classes breaking `kval--`, `callout--`, or `bf-` conventions found. File follows the project's CSS custom-property token system throughout.

## SQL  [PASS]
No `.sql` files modified in the last 24 hours.

## Brand Compliance  [PASS]
No issues found.

- "demo" / "Demo" / "TEST" / "sample": only matches are `"Demolition Supervisor"` (a legitimate PSIRA service category in `portal.js` line 8011) and an HTML comment `<!-- TESTIMONIALS -->` in `portal.php` line 338. Neither is UI-facing brand language.
- Software platform name: consistently referred to as "the Umlilo Portal" in all user-facing copy (portal.php lines 298, 303, 376, 394, 424, 449; portal.js nav labels). PHP file-level developer comment (`BlackFire Solutions Portal`) is a code comment, not a UI string.

## Files Changed in Last 24h
- BlackFire Portal/api/statements.php
- BlackFire Portal/api/task_users.php
- BlackFire Portal/install/deploy.sh
- BlackFire Portal/portal.css
- BlackFire Portal/portal.js
- BlackFire Portal/portal.php
- BlackFire Portal/tests/quote-calllog-layout-regression.ps1
- BlackFire Portal/tests/template-store-layout-regression.ps1
- BlackFire Portal/tests/template-store-layout-regression.ps1.bak-20260720-055650
- BlackFire Portal/tests/three-platform-quote-tracker-parity-regression.ps1
- BlackFire Portal/tests/tracker-reassignment-regression.ps1

## Verdict
Ready to commit ✓
