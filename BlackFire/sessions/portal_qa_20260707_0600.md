# Portal QA Report — 2026-07-07 06:00 SAST

## PHP Safety  [PASS]

No `echo $_GET`, `echo $_POST`, or `eval()` found in any live PHP file.

All API endpoints confirm `api_headers()` called before `require_auth()` / `require_perm()` (consistent with CLAUDE.md rule). RBAC guards verified across all checked endpoints:

- `api/audit.php` — `require_auth()`
- `api/callouts.php` — `require_auth()`
- `api/clients.php` — `require_auth()`
- `api/dashboard.php` — `require_auth()`
- `api/files.php` — `require_auth()`
- `api/finance.php` — `require_auth()`
- `api/invoices.php` — `require_auth()`
- `api/quotes.php` — `require_auth()`
- `api/admin.php` — `require_perm('security.users')`

All DB writes in live API files use parameterised prepared statements. No hardcoded credentials, passwords, or API keys found.

**Minor observation (FLAG, not BLOCK):** `api/admin.php` lines 38 and 113 build SQL UPDATE strings via string interpolation for the `export_password_sql` export feature. `$hash` is bcrypt output (ASCII-safe by design), and `$uname` is fetched directly from `bf_users` (not from user input at that point). The strings are returned as a review export, not executed directly — the actual DB updates on lines 44–47 and 78 use prepared statements. Risk is low, but the pattern could silently regress if `$uname` is ever sourced differently.

## JavaScript & CSS  [PASS]

**innerHTML:** Extensive use across portal.js, but all server-sourced or user-inputted values are consistently wrapped in the `esc()` escape helper before insertion. Spot-check of representative call at portal.js:392–404 confirms `esc(a.original_name)`, `esc(a.uploaded_by)`, `esc(a.mime_type)` all escaped. No raw user data assigned to innerHTML.

**console.log():** Zero matches in `portal.js`. Matches found only in `qa/qa_tracker_html.js` (a development QA script, not production code) — acceptable.

**CSS naming conventions:** `kval--`, `bf-`, and `kgrid--` BEM modifier classes are all used correctly per convention. No non-conforming double-hyphen class names introduced.

## SQL  [PASS]

No `.sql` files modified in the last 24 hours.

Live API PHP files reviewed: all `UPDATE` and `DELETE` statements include `WHERE` clauses with parameterised binding. No `DROP TABLE` or `TRUNCATE` present in live code paths.

## Brand Compliance  [PASS]

No instances of "demo", "Demo", "TEST", or "sample" found in `portal.php` or `portal.js` (live files). Matches in backup files were limited to the regulatory term "Demolition Supervisor" in the SHE safety checklist criteria — a statutory role reference, not brand language.

Platform naming check: `portal.php` uses "the Umlilo Portal" correctly on all first uses and throughout (lines 135, 296, 374, 421, 447). No alternative product name found.

## Files Changed in Last 24h

- None

## Verdict

Ready to commit ✓
