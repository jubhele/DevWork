# Portal QA Report — 2026-07-14 06:00 SAST

## PHP Safety  [PASS]

No raw `echo $_GET` / `echo $_POST` / `eval()` in any production PHP file.

All `$_GET` / `$_POST` reads in API endpoints (`api/admin.php`, `api/safety_compliance.php`, `api/finance.php`, `api/invoices.php`, `api/safety.php`, `sign.php`, `policy_ack.php`) are wrapped in the `clean()` helper or cast to `(int)` before use. String action parameters are consumed only in strict equality comparisons and never interpolated into SQL.

`require_perm()` guards are present on all admin and security endpoints checked.

**Note (non-blocking):** `api/admin.php` line 38 constructs SQL strings with bcrypt hashes for *display* (returned to the admin UI for review). Actual DB mutations on line 44 use parameterised statements. The inline comment acknowledges bcrypt output is ASCII-safe. No injection risk, but the display SQL strings are unnecessary and could be removed in a future cleanup pass.

No hardcoded credentials, passwords, or API keys found in any `.php` file.

No issues found in `portal.php` or `router.php`.

## JavaScript & CSS  [PASS]

**innerHTML safety:** `esc()` (portal.js:9026) implements safe DOM-based escaping:
`d.textContent = String(s); return d.innerHTML;`
All dynamic innerHTML assignments reviewed (client labels, callout/quote/invoice dropdown options, file attachment names, service grid, category cards) route user-supplied strings through `esc()`. No unsanitised assignments found.

**console.log:** Zero occurrences found in portal.js.

**CSS naming conventions:** Classes reviewed follow the established system (`kval`, `kcard`, `pnitem`, `saf-`, `pill`, `btn`, etc.). No rogue `kval--`, `callout--`, or `bf-` naming violations found.

## SQL  [PASS]

No `.sql` files were modified in the last 24 hours. No UPDATE/DELETE/DROP/TRUNCATE patterns to audit.

## Brand Compliance  [PASS]

Regex scans for `demo`, `Demo`, `TEST`, `sample` returned only:
- "Demolition Supervisor" (portal.js:6234, safety_doc_gen.php) — a CR14(1) statutory appointment title, not brand language. False positive.
- `<!-- ── TESTIMONIALS ── -->` (portal.php:337, portal.css:302) — section comment, not UI-facing.

No literal "demo", "TEST", or "sample" strings appear in UI-facing labels, headings, or user-visible copy.

Portal is consistently referred to as **"the Umlilo Portal"** throughout portal.php (lines 297, 301, 375, 422, 448) and in the nav button (line 136). Naming convention upheld.

## Files Changed in Last 24h

- sessions/portal_qa_20260713_0600.md
- sessions/token_digest_weekly.md

No PHP, JavaScript, CSS, or SQL production files were modified.

## Verdict

Ready to commit ✓
