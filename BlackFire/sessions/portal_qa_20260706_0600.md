# Portal QA Report — 2026-07-06 06:00 SAST

## PHP Safety  [PASS]

No issues found.

- `portal.php`: No raw `echo $_GET` / `echo $_POST` / `eval()` detected. All PHP variable output uses `htmlspecialchars()`, `json_encode()`, or explicit integer casts — no direct reflection of request parameters.
- No hardcoded credentials, passwords, or API keys found in `portal.php` or `router.php`.
- `router.php` is guarded by `PHP_SAPI !== 'cli-server'` — will not execute in a web context.
- RBAC enforcement: `portal.php` is a static HTML shell only; all authenticated data operations are routed through `api/*.php` endpoints via `fetch()`. No RBAC gaps in the shell itself.

## JavaScript & CSS  [PASS]

No issues found.

- **innerHTML / XSS audit**: All `innerHTML` assignments in `portal.js` that incorporate API-sourced or user-derived data consistently wrap values in the `esc()` helper (defined at line 1260), which escapes `&`, `<`, `>`, and `"`. No unsanitised data path to the DOM was found.
  - Ticker (`line 2463`): `items + items` — safe; `items` is derived from the hardcoded `SERVICES` constant, each entry escaped via `esc()`.
  - Dropdown population (`lines 667–730`): all label and value interpolation uses `esc()`.
- **console.log**: Zero occurrences in `portal.js`. (Hits in `_backups/` and `qa/` are expected and excluded from production scope.)
- **CSS naming**: No BEM-modifier class names violating `kval--`, `callout--`, or `bf-` conventions were introduced — no portal files were modified in the last 24 hours.

## SQL  [PASS]

No issues found.

No `.sql` files were modified in the last 24 hours. No SQL to audit.

## Brand Compliance  [PASS]

No issues found.

- Zero occurrences of `demo`, `Demo`, `TEST`, or `sample` in `portal.php`, `portal.js`, `portal.css`, or `router.php`.
- Product is consistently named **"the Umlilo Portal"** across all user-facing copy (nav button at line 135, About section at lines 296/300, FAQ at line 374, deployment step at line 392, footer at lines 421/447).

## Files Changed in Last 24h

- None

## Verdict

Ready to commit ✓
