# Portal QA Report — 2026-07-11 06:00 SAST

## PHP Safety  [PASS]
No issues found.

- No `echo $_GET` / `echo $_POST` / `eval()` present in portal.php or any api/*.php endpoint.
- All API endpoints follow the required order: `api_headers()` called before `require_auth()` / `require_perm()` on every file reviewed.
- No hardcoded credentials, passwords, or API keys found. All secrets route through `config/config.php` (gitignored).
- approvals.php GET/PATCH routes are intentionally public (token-based approval workflow) — correct by design.

## JavaScript & CSS  [PASS]
No blocking issues. One low-severity observation noted.

**Observation (non-blocking):** `portal.js:5062` — the audit-log renderer injects `fmtDT(e.ts).replace(', ', '<br>')` into `innerHTML` without passing through `esc()`. `fmtDT` calls `toLocaleString('en-ZA', {...})` on a server-generated timestamp, so in practice no user-controlled data reaches this path and XSS is not a realistic risk. However, for strict consistency with the codebase pattern, the output should be escaped before the `.replace()` call.

- No `console.log()` calls left in non-debug code paths.
- All data-driven `innerHTML` assignments in changed code use `esc()` consistently.
- CSS changes: modifications to existing classes only (`.panel`, `.kval`, `.kgrid`, `.twocol`, `.audit-row`, `.audit-ts`, `.audit-user`, `.ksub`). No new classes added. No violations of `kval--`, `callout--`, or `bf-` naming conventions.
- Nav change: `p-clients` (Clients page) moved from Finance group to Operations group — routing fix, no security impact.

## SQL  [PASS]
No SQL files modified in the last 24 hours.

## Brand Compliance  [PASS]
No issues found.

- Platform referred to as "the Umlilo Portal" throughout portal.php (About section, FAQ, footer). ✓
- No "demo", "Demo", "TEST", or "sample" strings in UI-facing code.
- `portal.js:6234` contains "Demolition Supervisor" — this is a safety-file checklist criterion label for construction compliance (OHS Act), not brand language.
- `portal.php:337` `<!-- ── TESTIMONIALS ── -->` is a developer comment, not UI-facing.

## Files Changed in Last 24h
- `BlackFire Portal/portal.css` — responsive layout refactor: `#pmain` max-width raised to 1500px, `.kgrid` switched to `auto-fit`/`minmax`, `.panel` gets `overflow:hidden`, `.audit-row` grid columns made fluid, mobile breakpoint cleanup.
- `BlackFire Portal/portal.js` — `p-clients` nav item moved from Finance to Operations group; audit-row timestamp split across two lines via `<br>` replace.
- `apps/web/src/app/(portal)/dashboard/page.tsx` — Next.js dashboard page update.
- `apps/web/src/app/(portal)/ops/page.tsx` — Operations page update.
- `apps/web/src/app/(portal)/secure/incidents/page.tsx` — Incidents page update.
- `apps/web/src/app/(portal)/secure/vault/page.tsx` — Vault page update.
- `apps/web/src/app/globals.css` — Global CSS update.
- `apps/web/src/components/PortalShell.tsx` — Portal shell component update.
- `apps/web/src/components/PowerBIReport.tsx` — Power BI report component update.
- `apps/web/src/components/report/ReportFrame.tsx` — Report frame update.
- `apps/web/src/lib/data/finance.ts` — Finance data update.

## Verdict
Ready to commit ✓
