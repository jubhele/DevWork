# Portal QA Report — 2026-07-19 06:00 SAST

## PHP Safety  [PASS]
No issues found.

- No raw `echo $_GET` / `echo $_POST` / `echo $_REQUEST` in portal.php or router.php.
- No `eval()` calls detected in any .php file.
- No hardcoded credentials, passwords, or API keys found.
- portal.php is a pure HTML shell; all data flows through `fetch()` → `api/*.php` endpoints. No direct DB queries, no SQL exposure in this file.
- router.php correctly gates itself: `if (PHP_SAPI !== 'cli-server') die(...)` — not accessible in production.
- No $_GET/$_POST used without sanitisation in the reviewed files.

## JavaScript & CSS  [PASS]
No issues found.

- **innerHTML safety confirmed.** The global `esc()` helper (portal.js:1412) correctly escapes all five HTML metacharacters: `&`, `<`, `>`, `"`. Every `innerHTML` assignment that interpolates server-returned data (callout rows, tracker updates, file listings, nav labels) runs values through `esc()`. No unescaped server data assigned to innerHTML detected.
- **No console.log() in production code paths.** `console.log()` appears only in `qa/qa_tracker_html.js` (an internal QA test runner script, not loaded by the portal). Zero occurrences in portal.js.
- **CSS naming conventions intact.** No new classes were introduced in the last 24 h; the existing `kval--`, `bf-`, `.calllog-`, `.pill`, `.pnav-` prefixes all comply with the established conventions. No bare or colliding class names detected.

## SQL  [PASS]
No SQL files modified in the last 24 hours.

(Historical install/ and _backups/ SQL files contain DROP TABLE / DELETE / UPDATE operations, but none were touched in this window and they are not part of a runtime code path.)

## Brand Compliance  [PASS]
No issues found.

- The string "demo" appears only in `portal.js:7436` as `'Demolition Supervisor'` — a South African construction-regulation role label (Construction Regulations CR14(1)), not a placeholder or demo label.
- "TESTIMONIALS" appears as an HTML section comment in portal.php and a CSS comment label; it is not UI-facing copy.
- The platform is consistently referred to as **"the Umlilo Portal"** across portal.php (lines 136, 297, 301, 375, 393, 449) in all public-facing and portal-shell copy. No alternate names or inconsistent product references detected.
- No strings matching 'TEST' or 'sample' found in UI-facing code.

## Files Changed in Last 24h
- sessions/portal_qa_20260718_0600.md  (yesterday's QA report — not portal code)

## Verdict
Ready to commit ✓
