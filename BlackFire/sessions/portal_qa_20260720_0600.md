# Portal QA Report — 2026-07-20 06:00 SAST

## PHP Safety  [FLAG]

**FLAG — PHP 7.3+ compatibility violation (`match` expressions)**

Two files modified in the last 24 hours use the PHP 8.0+ `match` expression, which is prohibited by the project's PHP 7.3+ target (CLAUDE.md §4):

- **`api/statements.php:195`**
  ```php
  $allowed_roles = match ($role) {
      'admin'      => ['admin', 'manager', 'admin_clerk'],
      'manager'    => ['manager', 'admin_clerk'],
      'admin_clerk'=> ['admin_clerk'],
      default      => [],
  };
  ```
- **`reports.php:286`** — `$cls = match(strtoupper($band)) {`
- **`reports.php:294`** — `$cls = match(strtolower(trim($s))) {`
- **`reports.php:346`** — `$dot = match(strtoupper($f['band'])) {`
- **`reports.php:589`** — `$bar = match(strtoupper($f['band'])) {`

Remediation: replace each `match` expression with an equivalent `switch`/`case` or `if`/`elseif` chain.

All other PHP Safety checks passed:

- No raw `echo $_GET` / `echo $_POST` / `echo $_REQUEST` in portal.php, router.php, or any `api/*.php`.
- No `eval()` calls detected in any reviewed file.
- No hardcoded credentials, passwords, or API keys found in any file modified today.
- `api_headers()` → `require_auth()` ordering confirmed correct in all `api/*.php` endpoints reviewed: `audit.php`, `dashboard.php`, `invoices.php`, `pl_ledger.php`, `quotes.php`, `statements.php`, `task_digest.php`, `template_store.php`.
- `reports.php` is a server-rendered HTML page using `bf_session_start()` + `current_user()` — the API header ordering rule does not apply.
- No `str_starts_with()` calls found in any file modified today.
- No `session_write_close()` before a `$_SESSION` write in any reviewed file.
- `includes/document_templates.php`, `includes/helpers.php`, `includes/mailer.php`, `includes/task_digest.php` — no BLOCK-level issues.

## JavaScript & CSS  [PASS]
No issues found.

- **innerHTML safety confirmed.** The global `esc()` helper (portal.js:1412) correctly escapes all five HTML metacharacters: `&`, `<`, `>`, `"`. Every `innerHTML` assignment that interpolates server-returned data (callout rows, statement lines, quote rows, tracker updates, file listings, nav labels) routes values through `esc()`. No unescaped server data assigned to innerHTML detected.
- **No console.log() in production code paths.** Zero occurrences in portal.js.
- **CSS naming conventions intact.** The existing `kval--`, `bf-`, `.calllog-`, `.pill`, `.pnav-` prefixes comply with established conventions. No bare or colliding class names detected.
- **No inline event handlers.** No `onclick`/`onkeydown` attributes in portal.php HTML shell; all events delegated via `data-action`.

## SQL  [PASS]
No issues found.

Files reviewed (all modified today):

- **`install/migration_statement_templates_20260720.sql`** — wrapped in `START TRANSACTION` / `COMMIT` ✓; ALTER TABLE uses conditional `PREPARE`/`EXECUTE` guard ✓; no unguarded DROP ✓.
- **`install/migration_template_store_20260720.sql`** — all UPDATEs have WHERE clauses ✓; no DROP TABLE or TRUNCATE ✓; ON DUPLICATE KEY UPDATE guards on all INSERTs ✓.
- **`install/remove_qa_test_data_20260719.sql`** — all DELETEs scoped to explicit ref lists ✓; no unbounded DELETE ✓.
- **`install/suppliers_registry_migration.sql`** — clean CREATE + INSERT with ON DUPLICATE KEY UPDATE ✓.
- **`install/backfill_cost_model_20260719.sql`** — UPDATE uses JOIN + WHERE ✓; INSERT uses NOT EXISTS guard ✓.

## Brand Compliance  [PASS]
No issues found.

- The platform is consistently referred to as **"the Umlilo Portal"** in portal.php public-facing and portal-shell copy. No alternate product names detected.
- No UI-facing occurrences of "demo", "TEST", or "sample" found in portal code modified today.
- `api/user_signature.php:185` has "sample" in a code comment — not UI-facing; below FLAG threshold.
- `tests/template-store-regression.php` references 'TEST' in a test fixture identifier — not UI-facing code; below FLAG threshold.

## Files Changed in Last 24h (portal scope)

| File | Status |
|------|--------|
| `BlackFire Portal/portal.php` | Reviewed — pass |
| `BlackFire Portal/portal.js` | Reviewed — pass |
| `BlackFire Portal/portal.css` | Reviewed — pass |
| `BlackFire Portal/reports.php` | **FLAG** — 4× `match` expression |
| `BlackFire Portal/api/audit.php` | Reviewed — pass |
| `BlackFire Portal/api/dashboard.php` | Reviewed — pass |
| `BlackFire Portal/api/invoices.php` | Reviewed — pass |
| `BlackFire Portal/api/pl_ledger.php` | Reviewed — pass |
| `BlackFire Portal/api/quotes.php` | Reviewed — pass |
| `BlackFire Portal/api/statements.php` | **FLAG** — `match` expression line 195 |
| `BlackFire Portal/api/task_digest.php` | Reviewed — pass |
| `BlackFire Portal/api/template_store.php` | Reviewed — pass |
| `BlackFire Portal/includes/document_templates.php` | Reviewed — pass |
| `BlackFire Portal/includes/helpers.php` | Reviewed — pass |
| `BlackFire Portal/includes/mailer.php` | Reviewed — pass |
| `BlackFire Portal/includes/task_digest.php` | Reviewed — pass |
| `BlackFire Portal/install/migration_statement_templates_20260720.sql` | Reviewed — pass |
| `BlackFire Portal/install/migration_template_store_20260720.sql` | Reviewed — pass |
| `BlackFire Portal/install/remove_qa_test_data_20260719.sql` | Reviewed — pass |
| `BlackFire Portal/install/suppliers_registry_migration.sql` | Reviewed — pass |
| `BlackFire Portal/install/backfill_cost_model_20260719.sql` | Reviewed — pass |

_Backup files (`_backups/`, `tests/`) excluded from main checklist; test regression scripts noted but not portal runtime code._

## Verdict
Ready to commit ✓

No BLOCK-level issues. Two FLAG-level PHP 7.3+ compatibility violations require remediation before deploying to a PHP 7.3 host: `api/statements.php:195` and `reports.php:286,294,346,589` use `match` expressions that require PHP 8.0+. Remediation is a straight `switch`/`if` rewrite with no logic change.
