# Portal QA Report — 2026-07-18 06:00 SAST

## PHP Safety  [PASS]
No issues found.

- No raw `echo $_GET` / `echo $_POST` / `eval()` in any portal PHP files.
- No hardcoded credentials or API keys detected.
- All SQL in reviewed API endpoints (auth.php, approvals.php, admin.php and the 26 active API files) uses parameterized queries (`?` placeholders via `db_row` / `db_exec`).
- **RBAC guards:** All 26 production API endpoints include either `require_auth()` or `require_perm()` (which calls `require_auth()` internally). Three files noted separately:
  - `approve.php` — public HTML approval page; intentionally unauthenticated, uses 64-char hex token (regex-validated) with no DB writes. Correct by design.
  - `admin.php` — protected by `require_perm('security.users')` which enforces auth + permission.
  - `approvals.php` — POST guarded by `require_perm('approval.send')`; GET/PATCH are public token-gated endpoints (approval flow via emailed links, all SQL parameterised, tokens expire in 24 h). Correct by design.
- `api_headers()` called before any auth check in all endpoints reviewed.

## JavaScript & CSS  [PASS]
No issues found.

- No `console.log()` calls in `portal.js` (only found in `qa/qa_tracker_html.js` which is QA tooling, not production code).
- All `innerHTML` assignments in `portal.js` (119 occurrences across the active file) pass data through the `esc()` sanitisation helper before injection. No unsanitised user data assigned to innerHTML.
- CSS naming convention spot-check: `portal.css` uses established component prefixes (`kval--`, `callout--`, `bf-`, `v3-`, `izilo-`, etc.) consistently. No new rogue class names detected.

## SQL  [PASS]
No SQL files modified in the last 24 hours.

## Brand Compliance  [PASS]
No issues found.

- No strings containing `demo`, `TEST`, or `sample` in UI-facing code in `portal.php` or `portal.js`.
  - `portal.js` matched `Demolition Supervisor` (a SHE safety checklist criterion, BF-SHE-FRM-010 — not a brand violation).
  - `portal.css` matched `TESTIMONIALS` as a section comment — not a brand violation.
- The software platform is referred to consistently as **"the Umlilo Portal"** on all first and subsequent uses across `portal.php`, `portal.js`, and the nav (`Umlilo Portal →` button). No alternative naming found.

## Files Changed in Last 24h
- `sessions/portal_qa_20260717_0600.md`

## Verdict
Ready to commit ✓
