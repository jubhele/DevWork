⛔ BLOCKED — BLOCK-LEVEL ISSUES FOUND. DO NOT DEPLOY.

# Portal QA Report — 2026-07-22 06:00 SAST

## PHP Safety  [ISSUES FOUND]

**BLOCK — Hardcoded credentials committed to git**

`BlackFire Portal/blackfire_secrets.php` is git-tracked (confirmed via `git ls-files`) and contains live plaintext credentials:

| Key | Issue |
|-----|-------|
| `BF_MAIL_PASS` | Plaintext email password: `noreplySawubona#@1` |
| `BF_GITHUB_PAT` | Live GitHub Personal Access Token: `github_pat_11BUBA6IQ0m7D3...` |
| `BF_APP_KEY` | AES-256 master encryption key: `b7d1f957c7bfe9b9...` |
| `BF_DB_PASS_ENC` | Encrypted DB password (decryptable with the committed BF_APP_KEY above) |
| `BF_DB_USER` | Database username: `blackfm6w9f9_umlilo_admin` |

**Impact**: Any person with repo access can decrypt the database password, impersonate the mail account, and use the GitHub PAT to push code or access org resources.

**The previous QA report (2026-07-21) incorrectly stated "No hardcoded credentials, passwords, or API keys in any PHP file."** The file was present and git-tracked prior to that report and was missed.

**Required remediation (in order):**
1. Rotate the GitHub PAT immediately — revoke `github_pat_11BUBA6IQ0m7D3uYyRLc1p_...` in GitHub Settings → Developer settings → Personal access tokens.
2. Rotate `BF_MAIL_PASS` and `BF_APP_KEY` on the server.
3. Remove `blackfire_secrets.php` from git tracking:
   ```
   git rm --cached "BlackFire Portal/blackfire_secrets.php"
   echo "BlackFire Portal/blackfire_secrets.php" >> .gitignore
   ```
4. Purge the credential from git history (e.g. `git filter-repo` or BFG Repo Cleaner) — the credential is in history even after a `git rm`.
5. Move secrets to the home-directory path (`~/blackfire_secrets.php`, outside the web root and repo) as intended in `encrypt_secrets.php` comments.

Other PHP safety checks:
- `echo $_GET` / `echo $_POST` / `eval()`: not found in any reviewed PHP file.
- `portal.php` is a pure HTML shell; all PHP output uses `htmlspecialchars()`.
- `router.php`: dev-only, correctly guards against PHP built-in server misuse.
- No raw SQL string interpolation detected in portal.php or router.php.

## JavaScript & CSS  [PASS]
No issues found.

- `innerHTML` assignments: `esc()` is properly defined at line 1643 (global scope) and additional local definitions at lines 9186 and 10835. Applied consistently to all server-data values before `innerHTML` injection. All reviewed `openModal()` call-sites pass user data through `esc()` or use `textContent` for the title parameter.
- `console.log()`: zero matches in `portal.js`. Matches in `qa/qa_tracker_html.js` are in a QA-only test runner — not a production code path.
- CSS naming: no new CSS files modified in the last 24 hours; existing classes in `portal.css` follow project conventions.

## SQL  [PASS]
No `.sql` files modified in the last 24 hours.

## Brand Compliance  [PASS]
No issues found.

- `demo` / `Demo` / `TEST` / `sample`: only matches are `"Demolition Supervisor"` (a real PSIRA competency label in `portal.js` line 8011) and the HTML comment `<!-- TESTIMONIALS -->` in `portal.php` line 338. Neither is UI-facing brand language.
- Software platform name: consistently referred to as "Umlilo Portal" in all user-facing copy across `portal.php` and portal nav labels.

## Files Changed in Last 24h
- sessions/portal_qa_20260721_0600.md

_(No portal code files were modified in the last 24 hours.)_

## Verdict
BLOCKED — fix issues above ✗

**Immediate action required: rotate GitHub PAT and mail password, then remove `blackfire_secrets.php` from git tracking.**
