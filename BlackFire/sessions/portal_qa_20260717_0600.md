# Portal QA Report — 2026-07-17 06:00 SAST

## PHP Safety  [PASS]
No issues found.

- `api_headers()` called before `require_auth()` in all five modified API endpoints (quotes.php, invoices.php, payments.php, pl_ledger.php, files.php). ✓
- All user-supplied values pass through `clean()` or explicit casts `(int)`, `(float)` before use. ✓
- All SQL queries use PDO prepared statements via `db_exec()`, `db_row()`, `db_select()`, `db_insert()`. No raw `$_GET`/`$_POST` concatenated into queries. ✓
- No `echo $_GET`, `echo $_POST`, or `eval()` anywhere. ✓
- No hardcoded credentials, passwords, or API keys found. ✓
- portal.php is a pure HTML shell; all data access is via `fetch()` to API endpoints. The shell uses `htmlspecialchars()` on every `$cfg` value. ✓
- No inline `onclick`/`onkeydown` handlers; event binding uses `data-action` delegation throughout. ✓

## JavaScript & CSS  [PASS]
No issues found.

- No `console.log()` in portal.js. (`console.error()` on line 64 is within the API error handler and is appropriate.) ✓
- All `innerHTML` assignments that render user-controlled data go through the `esc()` helper (line 10260: textContent-based HTML escaping). Verified on lines 474–487 (attachment list), 808–870 (dropdown builders), 3697–3709 (dash alert strip). ✓
- `escH()` (line 8958) is also correctly defined as a string-replace HTML escaper for standalone use. ✓
- CSS class names reviewed across the first 200 lines; all classes follow established `pub-*`, `bf-*`, `izilo-*`, `kcard`/`kval`/`klbl`, `callout*` conventions. No convention violations found. ✓

## SQL  [PASS]
No issues found.

Files changed in last 24h:
- `migration_payment_reversal_20260716.sql` — `ALTER TABLE` only; no `UPDATE`/`DELETE`. ✓
- `sync_quote_totals_vat_20260716.sql` — `UPDATE bf_quotes ... WHERE ABS(...) > 0.01`. WHERE clause present. ✓
- `insert_july2026_statement_AI202607.sql` — All `UPDATE` statements carry WHERE clauses (line 228–232: `WHERE i.ref_id IN (...)`, line 236–237: `AND due_date = '2026-06-22'`, line 240–242: `WHERE ref_id = 'CO-BF-CP1723'`). ✓
- `backfill_quote_items_zero_display_20260716.sql` — `UPDATE bf_quote_items ... WHERE line_total IS NULL`. WHERE clause present. ✓
- No `DROP TABLE` or `TRUNCATE` in any modified SQL file. ✓

## Brand Compliance  [PASS]
No issues found.

- No strings containing `demo`, `Demo`, `TEST`, or `sample` in UI-facing code. (`Demolition Supervisor` on portal.js line 7436 is a PSIRA safety criteria entry — not a `demo`-labelled UI element.) ✓
- Platform consistently referred to as **the Umlilo Portal** in portal.php (lines 297, 375, 393, 422, 448) and throughout API docblocks. ✓

## Files Changed in Last 24h
- `BlackFire Portal/api/files.php`
- `BlackFire Portal/api/invoices.php`
- `BlackFire Portal/api/payments.php`
- `BlackFire Portal/api/pl_ledger.php`
- `BlackFire Portal/api/quotes.php`
- `BlackFire Portal/includes/helpers.php`
- `BlackFire Portal/install/backfill_quote_items_zero_display_20260716.sql`
- `BlackFire Portal/install/insert_july2026_statement_AI202607.sql`
- `BlackFire Portal/install/migration_payment_reversal_20260716.sql`
- `BlackFire Portal/install/sync_quote_totals_vat_20260716.sql`
- `BlackFire Portal/portal.css`
- `BlackFire Portal/portal.js`
- `BlackFire Portal/portal.php`
- *(various session logs, test scripts, backups, mobile/web app files — outside portal.php QA scope)*

## Verdict
Ready to commit ✓
