# Portal QA Report — 2026-07-03 06:00 SAST

## PHP Safety  [PASS]

Reviewed: `portal.php`, `router.php`, and all active `api/*.php` endpoints.

- **echo $_GET / $_POST / eval()**: No unguarded echo of superglobals, no `eval()` calls found in any active PHP file. PASS.
- **Raw superglobal usage in active files**:
  - `sign.php:46–48` — `$_POST['signature_image']` validated via `preg_match('/^data:image\/png;base64,([A-Za-z0-9+\/=]+)$/')` before use. Safe.
  - `reports.php:52` — `$export = isset($_GET['export'])` used as a boolean flag only, not interpolated into any query. Safe.
  - `api/clients.php:27` — `$_GET['active'] !== '0'` is a boolean comparison only. Safe.
  - `api/approve.php:10` — token truncated via `substr(..., 0, 64)` before use as lookup parameter. Safe.
- **Hardcoded credentials**: None found. `approve.php:70` echoes a per-request session token via `htmlspecialchars()` — not a hardcoded secret.
- **RBAC guards**: All 24 active `api/*.php` endpoints contain `require_auth` / `SESSION` / role checks. PASS.

## JavaScript & CSS  [PASS]

Reviewed: `portal.js` (full), `portal.css` (full).

- **innerHTML with unsanitised data**: All dynamic `innerHTML` assignments route server-returned strings through the `esc()` helper defined at line 8538: `function esc(s){var d=document.createElement('div');d.textContent=String(s==null?'':s);return d.innerHTML;}`. This is a proper DOM-based HTML escaper. No direct concatenation of raw API fields into `innerHTML` detected. PASS.
- **console.log()**: Zero occurrences found in `portal.js`. PASS.
- **CSS naming conventions**: All modifier classes follow the established BEM double-dash convention (`kval--lg`, `kval--compact`, `kval--paid`, `kval--ovr`, `kcard--flex`, `kcard--credits`, `kcard--debits`, `kcard--net`). `bf-` prefix classes (`bf-logo-dark`, `bf-logo-light`) are correctly namespaced. No violations. PASS.

## SQL  [PASS]

No `.sql` files were modified in the last 24 hours (`git log --since='24 hours ago'` returned no results). SQL section not applicable for this cycle.

## Brand Compliance  [PASS]

- **Demo / TEST / sample strings**: The only match for the pattern `demo|Demo` in active code is `portal.js:5826` — `criteria:'Demolition Supervisor'` — a legitimate English word inside the safety checklist schema, not a placeholder or brand violation.
- **'Umlilo Portal' naming**: The platform is correctly referred to as "the Umlilo Portal" on all public-facing first-use instances in `portal.php` (lines 296, 300, 374, 421, 447) and in the public nav button (line 135). PASS.

## Files Changed in Last 24h

- None

## Verdict

Ready to commit ✓
