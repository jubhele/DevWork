# Portal QA Report — 2026-07-09 06:00 SAST

## PHP Safety  [PASS]

No issues found.

- `portal.php`: All dynamic output uses `htmlspecialchars()` / `json_encode()`. No raw `$_GET`/`$_POST` echoed.
- `router.php`: Dev-only routing helper; no user data processed.
- `config/config.php` *(changed in last 24h)*: Credentials sourced exclusively from environment variables, `blackfire_secrets.php` (outside web root), or `.env` (dev fallback). No hardcoded credentials. New `cfg_has_value()` helper and dual-secrets-file lookup are safe.
- `includes/db.php` *(changed in last 24h)*: All queries use PDO prepared statements via `db_select()`, `db_row()`, `db_exec()`, `db_insert()`. No raw SQL from user input.
- All API files (`api/*.php`): `api_headers()` is called before `require_auth()` / `require_perm()` on every endpoint — ordering rule satisfied. `require_perm()` internally calls `require_auth()` (confirmed in `includes/auth.php:215`).
- No `eval()` calls found in any production PHP file.
- Minor observation (not BLOCK): `api/admin.php:38` builds a SQL preview string via PHP interpolation — but the values are `password_hash()` output (ASCII-safe, bcrypt format) and DB-sourced usernames. The string is returned for display only; the actual DB update on lines 44–47 uses prepared statements. No remediation required.

## JavaScript & CSS  [PASS]

No issues found.

- `portal.js`: Zero `console.log()` calls found across 8,873 lines.
- `portal.js`: All `innerHTML` assignments that render dynamic data pass values through the `esc()` helper (line 8638 — uses `textContent` encoding) or `escH()` (line 7350 — regex-based). No innerHTML from raw user input detected.
- `portal.css`: No CSS class names violating `kval--`, `callout--`, or `bf-` naming conventions were introduced.
- `portal.css`, `portal.js`, `portal.php`: No "demo", "TEST", or "sample" strings in UI-facing code or comments.

## SQL  [PASS]

No SQL files modified in last 24h — checks not applicable.

(Standing note for awareness, not blocking today: `install/blackfire_real_finance_data.sql:33` contains `DELETE FROM bf_transactions;` with no WHERE clause — an intentional full-table wipe in a seeding script, not a migration. This file was not changed in the last 24h and is in `install/` only.)

## Brand Compliance  [PASS]

No issues found.

- No occurrences of "demo", "Demo", "TEST", or "sample" in UI-facing code or inline comments in `portal.php`, `portal.js`, `portal.css`, `config/config.php`, or `includes/db.php`.
- The platform is consistently referred to as "the Umlilo Portal" across `portal.php` (FAQ section, About section, footer copy), `config/config.php` (`app_name` key), and all API file headers.

## Files Changed in Last 24h

Production portal files:
- `BlackFire Portal/config/config.php` — dual-secrets-file lookup + `cfg_has_value()` helper added
- `BlackFire Portal/includes/db.php` — minor update (see git diff)

Other (non-portal or non-production):
- `apps/web/` — Next.js web app changes (PowerBI embed, finance page)
- `sessions/`, `memory/`, `CLAUDE.md`, `AGENTS.md` — housekeeping
- `BlackFire Portal/install/_backups/`, `scripts/`, `tests/` — deploy/restore scripts and regression test

## Verdict

Ready to commit ✓
