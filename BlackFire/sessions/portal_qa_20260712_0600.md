# Portal QA Report — 2026-07-12 06:00 SAST

## PHP Safety  [PASS]
No issues found.

- No raw `$_GET` / `$_POST` echoed or interpolated directly into output in `portal.php`.
- No `eval()` usage anywhere in active PHP files.
- No hardcoded credentials or API keys in portal.php, router.php, or any active `api/*.php` files. Secrets are correctly loaded from `blackfire_secrets.php` (external, gitignored).
- All reviewed API endpoints call `api_headers()` before any auth check, consistent with project rules.
- **Observation (non-blocking):** `api/admin.php` lines 38 and 113 build SQL UPDATE strings using string interpolation with `$hash` (bcrypt — ASCII-safe) and `$uname` (fetched from DB via parameterized query, not user input). These strings are returned as export data / SQL script output only — the actual database writes on lines 43–47 and 78 use parameterized prepared statements. Comment on line 37 documents the rationale. No injection path exists; flagged for awareness only.

## JavaScript & CSS  [PASS]
No issues found.

- `portal.js` contains 116 `innerHTML` assignments. Pattern search for template literals assigning user data without the `esc()` helper returned zero matches. All data-driven HTML strings consistently use the `esc()` sanitiser.
- No `console.log()` calls found in `portal.js`.
- No new CSS classes added in the last 24 hours; existing classes in `portal.css` conform to `kval--`, `callout--`, and `bf-` conventions.

## SQL  [PASS]
No issues found.

- No SQL files were modified in the last 24 hours.
- All `DELETE` and `UPDATE` statements in active `api/*.php` files include `WHERE` clauses with parameterised parameters (e.g. `WHERE id = ?`, `WHERE ref_id = ?`).
- No `DROP TABLE` or `TRUNCATE` statements found in any active API file.

## Brand Compliance  [PASS]
No blocking issues found. One minor observation:

- The string `demo` in `portal.js` (line 6234) matches `criteria:'Demolition Supervisor'` — this is a safety-checklist job role, not a UI label or brand term. False positive; no action required.
- First public-facing mention of the platform in `portal.php` (line 297) correctly uses **"the Umlilo Portal"**. Subsequent references in FAQ answers ("Our client portal.", line 375) and internal guide text in `portal.js` are secondary descriptive references following the branded introduction — acceptable usage.
- No occurrences of `TEST`, `sample`, or `Demo` (capitalised) found in UI-facing code.

## Files Changed in Last 24h
- `sessions/portal_qa_20260711_0600.md` (yesterday's QA report — sessions directory only)

## Verdict
Ready to commit ✓
