// Minimal placeholder portal.js for visual verification only.
// The original portal runtime depends on backend APIs and CSP nonce.
console.log('portal.js placeholder loaded — original portal.js not integrated');
document.addEventListener('DOMContentLoaded', function(){
  document.documentElement.dataset.state = document.documentElement.dataset.state || 'public';
});
