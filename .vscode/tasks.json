{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Close Session Log",
      "type": "shell",
      "command": "powershell.exe",
      "args": [
        "-NonInteractive",
        "-File",
        "${workspaceFolder}\\.claude\\scripts\\session-log-update.ps1",
        "-LogPath",
        "${input:sessionLogPath}"
      ],
      "group": "none",
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "showReuseMessage": false
      },
      "problemMatcher": [],
      "detail": "Manual recovery for providers or surfaces whose native lifecycle hook did not run. Validates the latest log and writes a signature only when Goal Status = ACHIEVED."
    }
  ],
  "inputs": [
    {
      "id": "sessionLogPath",
      "type": "promptString",
      "description": "Exact absolute path of the session log to close (never use newest-by-date discovery)"
    }
  ]
}
