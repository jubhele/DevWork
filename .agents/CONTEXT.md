# DevWork Antigravity Constitution Context

Before substantive work on the first prompt, and again on every later prompt:

1. Read `CLAUDE.md`, `AGENTS.md`, the shared memory index, and the exact active session log.
2. Run uSibali tier/model clearance and route the task through uMlawuli before a Sebenza agent acts.
3. Keep the session log, accountability ledger, backups, Learnings, and mirror current.
4. Keep Goal Status PENDING until the user explicitly confirms ACHIEVED.
5. Determine the owning project. If unclear, ask the user to choose an existing project, create a new named project, or explicitly select `_workspace`; do not generate artifacts while unresolved.
6. Use lowercase-u Zulu proper names such as `uSibali`, `uMlawuli`, `uMakhi`, and `uMlindi`.

Antigravity's verified project hook gates mutation tools through `.agents/hooks.json`. This context file is the start/every-prompt fallback; do not claim a distinct native prompt event unless the installed Antigravity version is live-tested and documented.
